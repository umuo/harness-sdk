# Agent 开发规范

本文件是本项目面向编码 Agent 的规范唯一来源。任何 Agent 在分析、修改、测试或提交代码前，都必须完整阅读并遵守本文。若子目录存在更具体的 `AGENTS.md`，则子目录规则在其作用域内优先，但不得降低本文的质量和安全要求。

## 1. 项目定位与模块边界

本项目是轻量级 Java 8 LLM Agent Harness SDK，采用 Maven 多模块结构：

- `agent-core`：与模型提供商无关的 Agent、State、Tool、Plugin、Todo、Skill 和可观测性核心。不得依赖具体 HTTP 提供商、MCP 实现或工作区文件系统 Tool。
- `agent-model-http`：Java 8 HTTP/SSE 传输，以及 OpenAI、Anthropic 等模型适配器。
- `agent-mcp`：MCP 客户端、stdio 传输、Tool 发现与本地适配。
- `agent-tools-builtin`：受工作区约束的文件、补丁、Glob 和可选 Bash Tool。
- `agent-examples`：可运行示例；示例不能成为核心模块的反向依赖。
- `agent-observability-web`：独立 Web 项目，不属于 Maven Reactor。

修改前先确认职责归属，禁止为了方便破坏模块方向。公共抽象优先放在 `agent-core`，提供商细节留在对应适配模块，文件系统与命令执行能力留在 `agent-tools-builtin`。

## 2. 开始修改前

1. 阅读相关源码、测试和 `docs/` 文档，先理解现有契约再动手。
2. 执行 `git status --short`，区分已有改动与本次改动。不得覆盖、清理或重写不属于当前任务的修改。
3. 使用 `rg` / `rg --files` 查找实现、调用方和测试，避免只改一个入口而遗漏兼容路径。
4. 优先做范围最小、职责完整的修改；不得借任务之名进行无关重构、格式化全仓代码或升级依赖。
5. 修改公共 API、Tool 名称、JSON 字段、错误码或默认值前，必须检查兼容性并同步测试和文档。

## 3. Java 8 强制约束

- 源码必须兼容 Java 8，不得使用 Java 9 及以上的语言特性或标准库 API，例如 `record`、文本块、模式匹配、`List.of`、`Map.of`、`String.isBlank`、`Files.readString`。
- 异步逻辑使用现有的 `CompletableFuture`、Executor 和 Scheduler 抽象，不得私自引入新的并发运行时。
- 字符编码必须显式使用 `StandardCharsets.UTF_8` 等常量，不依赖平台默认编码。
- 时间、随机数、线程池和外部 I/O 应可注入或可控，避免难以复现的测试。
- 新依赖必须有明确必要性；能用 JDK 或现有依赖完成时，不新增第三方库。

完整构建必须通过根 `pom.xml` 中的 Java 8 animal-sniffer 检查。

## 4. 中文注释规范

- 新增或修改的复杂类、公共 API、核心算法、状态转换、并发控制、安全边界和非显然分支，必须补充准确的中文 Javadoc 或行内注释。
- 注释重点解释“为什么这样设计”“约束是什么”“失败后如何恢复”，不要逐行翻译代码或复述变量名。
- Tool 的路径校验、大小限制、截断、回滚、锁顺序、先读后改等安全语义必须有中文注释。
- 公共类和公共方法优先使用中文 Javadoc；面向模型的 Tool 描述、参数名和协议文本可按既有英文契约保持稳定。
- 修改代码时同步更新已有注释。失效、含糊或与实现矛盾的注释视为缺陷。
- 不写无信息量注释，例如“设置变量”“进入循环”“返回结果”；简单 getter、显然构造器无需强制注释。

## 5. Sonar 友好的代码质量规则

所有新增或修改代码应以不引入 Sonar Blocker、Critical、Major 问题为最低目标。即使本地未配置 Sonar 服务，也必须遵守以下规则。

### 5.1 可读性与复杂度

- 类和方法保持单一职责；复杂方法拆为有语义的私有方法。
- 优先使用提前返回降低嵌套，正常情况下嵌套层级不超过 3 层。
- 避免过长方法、过长参数列表、复杂布尔表达式和高认知复杂度；必要时引入值对象或内部上下文对象。
- 消除重复实现，复用已有帮助类；不要为一次简单调用制造无意义抽象。
- 魔法数字、协议标记、默认上限和重复字符串应提取为有名称的常量。
- 删除未使用的导入、变量、字段、方法和不可达代码；不得保留被注释掉的旧实现。
- 禁止通配符导入；命名必须表达业务含义，避免无意义缩写和单字母名称（短循环索引除外）。

### 5.2 异常与资源

- 捕获尽可能具体的异常；除真正的系统边界外，不得直接捕获 `Exception` 或 `Throwable`。
- 不得吞掉异常、仅打印堆栈或返回虚假成功。转换异常时保留原始 cause，并提供可恢复信息。
- 捕获 `InterruptedException` 后，如不继续抛出，必须调用 `Thread.currentThread().interrupt()` 恢复中断标记。
- 文件、流、HTTP 响应和进程资源必须用 try-with-resources 或明确的 `finally` 关闭。
- 清理或回滚失败不能静默忽略；应保留为 suppressed exception、结构化错误或可观测事件。
- 库代码不得使用 `System.out` / `System.err` 输出运行信息；示例程序除外。使用项目既有结果、事件或可观测性抽象。

### 5.3 空值、集合与对象契约

- 在边界处使用 `Objects.requireNonNull`、参数校验或结构化错误尽早失败。
- 不返回 `null` 集合；返回空集合或 `Optional`，并保持现有 API 风格。
- 对外暴露的集合应防御性复制并保持不可变，避免调用方修改内部状态。
- 实现 `equals` 时必须同步实现 `hashCode`；比较字符串和包装类型时避免引用比较。
- 不把可变数组、集合或内部状态直接暴露给调用方。

### 5.4 并发与异步

- 共享可变状态必须有清晰的线程安全策略；锁顺序固定，避免死锁。
- 不在持锁期间执行不可控的网络请求、模型调用或长时间阻塞操作。
- `CompletableFuture` 链必须正确传播异常、取消和超时，不创建无人观察的异步任务。
- 禁止使用无上限线程池、无上限队列或忙等循环。
- 并发测试应使用 latch、future 或可控调度器，避免用固定 `sleep` 猜测时序。

### 5.5 安全与隐私

- 不得提交密钥、Token、密码、真实用户数据、私有端点或本机绝对路径。
- 日志、异常、ToolResult 和追踪内容不得泄露 Authorization、Cookie、请求密钥或完整敏感载荷。
- 所有外部输入必须验证类型、范围、大小和路径；文件路径必须遵守工作区与符号链接边界。
- 构造 shell 命令、URL、HTTP Header 和 JSON 时防止注入；优先使用结构化 API，不拼接不可信输入。
- 不得关闭 TLS 校验、信任所有证书或使用不安全的加密/随机算法。
- 输出必须有明确上限；大内容使用项目已有的分页、截断、溢出文件和 `ToolOutputReference` 机制。

### 5.6 Sonar 抑制规则

- 不得为了让扫描变绿而随意添加 `@SuppressWarnings`、`// NOSONAR` 或排除目录。
- 确需抑制时，必须把范围缩到最小，并用中文注释说明规则编号、误报原因和安全依据。
- 新增 Sonar 问题应修复根因；历史问题与当前改动直接相关时一并修复，否则在交付说明中明确记录。

## 6. Tool 开发专项规则

- 优先继承 `AbstractTool<I>` / `AbstractAsyncTool<I>`，使用强类型输入和 `@ToolParam` 生成 Schema，不手写易漂移的 JSON Schema。
- Tool 名称、面向模型的参数名和已公开错误码属于兼容契约。Java 字段使用 camelCase，模型参数按现有约定使用 snake_case。
- 失败使用 `ToolFailureException` 和 `ToolErrorInfo` 返回稳定错误码、是否可重试、恢复建议及必要细节；不得把 Java 堆栈直接写入模型消息。
- Tool 返回内容必须紧凑且有界。可能产生大输出的 Tool 应在生产阶段限制数据，并通过 `ToolOutputStore` / `ToolOutputReference` 保存可恢复的完整结果。
- 文件修改 Tool 必须复用 `WorkspacePathResolver`、`FileObservationTracker`、`PathLocks` 和 `AtomicFileWriter` 等已有安全设施。
- 默认保留“先读后改”和文件版本校验；不得绕过工作区、符号链接、大小预算或并发锁。
- Bash 不是安全沙箱。扩大命令执行能力、环境变量暴露或工作区外访问时，必须明确安全边界并增加测试和文档。
- Tool 成功与失败都应便于程序消费和观测；元数据字段保持稳定、紧凑，不放入无界内容。

## 7. 测试规范

- 每个行为变更必须有自动化测试；修复缺陷时先构造能复现缺陷的回归场景。
- 单元测试必须确定、隔离、可重复，不依赖真实网络、真实 LLM、用户主目录或执行顺序。
- 文件系统测试使用 JUnit `@TempDir`；测试结束不得在仓库外留下文件或进程。
- 覆盖正常路径、边界值、结构化错误、恢复建议、取消/超时、并发或回滚等与改动相关的失败路径。
- 不通过放宽断言、增加任意等待、禁用测试或捕获后忽略异常来“修复”测试。
- 真实模型示例可以因缺少凭证而跳过，但核心行为必须由脚本化模型或本地测试覆盖。

建议先运行受影响模块：

```bash
mvn -pl <module> -am test
```

交付前必须运行：

```bash
mvn verify
git diff --check
```

若仓库后续配置了 Sonar 服务、Quality Profile 和凭证，再运行项目约定的 Sonar 命令；未实际运行时不得声称 Sonar 扫描通过。

## 8. 文档与兼容性

- 新增公共能力、配置项、Tool、错误码或安全限制时，同步更新 `README.md` 或相关 `docs/*.md`。
- 示例代码必须可以编译，并与当前 API、Tool 名称和默认行为一致。
- 默认保持二进制、源码和模型协议兼容；必须破坏兼容时，先明确影响范围、迁移方式并获得任务授权。
- 文档描述应区分应用级防护与操作系统安全沙箱，不夸大事务、安全或可靠性保证。

## 9. Git 与交付

- 不执行 `git reset --hard`、覆盖式 checkout、强制推送或删除不属于当前任务的文件。
- 除非用户明确要求，否则不创建提交、不推送远端、不改写历史。
- 提交前检查 `git diff`、`git diff --check` 和测试结果，确保没有调试代码、密钥、生成物或无关文件。
- 最终说明应包含：实现结果、关键文件、验证命令与结果、已知限制；不得隐瞒失败测试或跳过项。

## 10. 完成检查清单

- [ ] 模块边界和现有公共契约未被意外破坏。
- [ ] 复杂逻辑与安全边界已有准确中文注释。
- [ ] 代码符合 Java 8，并通过 animal-sniffer。
- [ ] 未引入明显 Sonar 可靠性、安全性或可维护性问题。
- [ ] 异常、资源、并发、空值和输出大小已正确处理。
- [ ] 新增或修改行为已有正常与失败路径测试。
- [ ] 文档、示例、Schema、错误码与实现保持一致。
- [ ] `mvn verify` 和 `git diff --check` 通过。
- [ ] 工作区只包含本任务相关修改。
